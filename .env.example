# Semaphore instance base URL (no trailing /api)
SEMAPHORE_URL=https://semaphore.example.com

# API token — mint one on a scoped RBAC user (read + task-run only).
# Semaphore UI: User settings -> API Tokens. NEVER commit the real value.
SEMAPHORE_TOKEN=
